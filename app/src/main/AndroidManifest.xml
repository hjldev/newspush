<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.hjl.newspush">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme.NoActionBar">
        <activity
            android:name=".mvp.ui.activities.NewsActivity"
            android:label="@string/app_name"
            android:theme="@style/BaseAppTheme.NoTrans"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activities.ChannelActivity"
            android:theme="@style/BaseAppTheme.NoTrans"/>
        <activity android:name=".mvp.ui.activities.NewsDetailActivity"
            android:theme="@style/BaseAppTheme.NoTrans"/>
        <activity android:name=".mvp.ui.activities.NewsPhotoDetailActivity"
            android:theme="@style/BaseAppTheme.NoTrans"/>
        <activity android:name=".mvp.ui.activities.PhotoActivity"
            android:theme="@style/BaseAppTheme.NoTrans"/>
        <activity android:name=".mvp.ui.activities.PhotoDetailActivity"
            android:theme="@style/BaseAppTheme.NoTrans"/>
    </application>

</manifest>